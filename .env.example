OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# 推荐使用 PostgreSQL，因为当前 Prisma schema 使用 Json 字段。
DATABASE_URL=

# 本地 SQLite 示例：
# DATABASE_URL="file:./dev.db"
# 注意：如切换到 SQLite，请同步确认 Prisma provider 和 Json 字段兼容性。

NEXT_PUBLIC_APP_NAME="中国大陆法律 AI 工作台"
